---
import { socialLinks } from "@utils/social-links";
import HeaderLink from "@components/HeaderLink.astro";
---

<header class="bg-base-200 shadow-sm">
  <nav class="flex items-center justify-between px-4 container">
    <div class="flex items-center gap-4">
      <HeaderLink href="/">Home</HeaderLink>
      <HeaderLink href="/blog">Blog</HeaderLink>
      <HeaderLink href="/ts-university">TypeScript University</HeaderLink>
      <HeaderLink href="/snippets">Code Snippets</HeaderLink>
    </div>
    <div class="hidden md:flex items-center gap-4">
      {
        socialLinks.map((link) => (
          <a
            href={link.href}
            target="_blank"
            class="text-base-content/80 hover:text-base-content"
          >
            <span class="sr-only">{link.ariaLabel}</span>
            <link.Icon />
          </a>
        ))
      }
    </div>
  </nav>
</header>
